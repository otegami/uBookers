<div class="row">
<div class="cos-sm-9">
  <table>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
    <% @books.each do |book| %>
    <tr>
      <td><!-- Insert UserImage Here --></td>
      <td><%= link_to "#{book.title}", book_path(book) %></td>
      <td><%= book.opinion %></td>
    </tr>
    <% end %>
  </table>
</div>
</div>
